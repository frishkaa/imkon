# --- Web / API ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12

# --- Data / validation ---
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# --- DB ---
SQLAlchemy>=2.0.36
alembic>=1.14
psycopg[binary]>=3.2

# --- AI client (provider-agnostic: DeepSeek dev / local Qwen prod) ---
httpx>=0.27

# --- Security ---
cryptography>=43        # AES-256-GCM field encryption
argon2-cffi>=23         # password hashing

# --- PDF / profile ---
reportlab>=4.2          # pure-python PDF (no native GTK deps; Windows-safe)
qrcode[pil]>=8.0        # QR code back to live profile

# --- Telegram bot (stays off without TELEGRAM_BOT_TOKEN) ---
python-telegram-bot>=21.6

# --- Synthetic / test data ---
Faker>=30

# --- Tests ---
pytest>=8.3
pytest-cov>=5
